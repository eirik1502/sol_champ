package sol_engine.creator;

import sol_engine.ecs.World;

public class SolEditor {

    public static void applyEditor(World world) {
        useEditor(world, true);
    }

    public static void useEditor(World world, boolean hideEditorFromUser) {
        // add Graphics and input modules
        // add world listeners
    }
}
